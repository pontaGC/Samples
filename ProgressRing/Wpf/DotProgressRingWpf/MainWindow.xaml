<Window x:Class="DotProgressRingWpf.MainWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DotProgressRingWpf"
        mc:Ignorable="d"
        Title="DotProgressDialog-Wpf"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Height="150" Width="300"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <Button Width="100" Height="40"
                Content="Show Progress"
                Command="{Binding ShowProgressRingCommand, Mode=OneTime}"
                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" />
    </Grid>
</Window>
